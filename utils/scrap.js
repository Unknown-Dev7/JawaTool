// utils/scrap.js
const Axios = require("axios");
const cheerio = require("cheerio");
const qs = require("qs");
const FormData = require("form-data");

// Pindahin semua fungsi downloader ke sini
async function ssstik(url) {
   // isi kode yang lo udah punya
}

module.exports = { ssstik }; // export agar bisa dipakai di backend